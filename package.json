{
  "name": "paysons-movies",
  "version": "1.2.0",
  "private": true,
  "description": "Payson’s Movies - cinematic movie discovery and authorized multi-source playback.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/player.js"
  },
  "dependencies": {
    "hls.js": "^1.6.15"
  }
}
