{
  "name": "paysons-movies",
  "version": "1.1.0",
  "private": true,
  "description": "Payson’s Movies - a cinematic glass movie discovery UI with a zero-dependency Node backend.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  }
}
